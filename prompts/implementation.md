Produce a minimal implementation checklist and runnable experiment shell script.
Output two sections:
1) Markdown checklist
2) Shell script fenced block (bash) that can run on Linux aarch64 with Slurm environment.

Constraints:
- Public datasets.
- Keep runtime low.
- Save metrics to JSON under runs/<RUN_ID>/results.json.
